function logUserInput() {
    // Get the input element by its ID
    var userInput = document.getElementById("productName").value;

    // Log the user input to the console
    console.log("User input:", userInput);
}
